import numpy as np
from math import *
from numpy import *

one = np.array([1,2,3])
two = np.array([4,5,6])
three = np.array([7,8,9])

print sqrt(degrees(one)**2+arctan2(two,three)-1**2)

hap ={'one':1}
a = 'one'

if a in hap:
    print 'in'
#ATT.DesRoll {'RAD': <pymavlink.DFReader.DFMessage object at 0x7f2d1e07a5d0>,